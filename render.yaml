# render.yaml
services:
  - type: web
    name: reflex-logistics-api
    env: node
    buildCommand: npm install && npm run seed
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true